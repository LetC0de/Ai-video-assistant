# ── AI video Assistant ── requirements.txt ──────────────────────────────────
# Python >= 3.10 recommended

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2024.4.9                  # Download audio from YouTube URLs
pydub>=0.25.1                     # Audio file manipulation / format conversion
ffmpeg-python>=0.2.0              # FFmpeg bindings (FFmpeg binary must be installed separately)
youtube-transcript-api>=0.6.0    # Official YouTube caption fetch (used by the caption ingestion path)

# ── Speech-to-Text (local Whisper) ────────────────────────────────────────────
openai-whisper>=20231117          # OpenAI Whisper (runs locally)
torch>=2.2.0                      # PyTorch backend for Whisper
torchaudio>=2.2.0                 # Audio utilities for PyTorch

# ── Translation (Hindi → English) ─────────────────────────────────────────────
deep-translator>=1.11.4           # Lightweight translation wrapper (Google backend)

# ── LLM Orchestration — LangChain LCEL + OpenRouter ────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.3.0           # ChatOpenAI — used with OpenRouter API
langchain-mistralai>=0.1.0        # Mistral embeddings (still used for vector store)
mistralai>=0.4.0                  # Mistral Python client (embeddings backend)

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
qdrant-client                     # Qdrant Cloud vector store client
langchain-qdrant                  # LangChain ↔ Qdrant bridge
sentence-transformers>=3.0.0      # HuggingFace embedding models
langchain-huggingface>=0.0.3      # LangChain ↔ HuggingFace embeddings bridge
huggingface-hub>=0.23.0           # Model downloading from HuggingFace Hub
tiktoken>=0.7.0                   # Token counting for text splitting


# ── PDF & TXT Export ──────────────────────────────────────────────────────────
reportlab>=4.2.0                  # PDF generation
fpdf2>=2.7.9                      # Lightweight alternative PDF library

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0              # Load API keys from .env
numpy>=1.26.0
tqdm>=4.66.0                      # Progress bars
requests>=2.32.0

# ── API Server (FastAPI) ─────────────────────────────────────────────────────
fastapi>=0.110.0                  # Web framework / REST API
uvicorn>=0.29.0                   # ASGI server
python-multipart>=0.0.9           # Form/file upload support
pydantic[email]>=2.6.0            # Schemas + EmailStr validation
pydantic-settings>=2.2.0          # Typed settings from .env

# ── Database (Postgres + migrations) ──────────────────────────────────────────
sqlalchemy>=2.0.0                 # ORM
psycopg2-binary>=2.9.0            # Postgres driver (Neon)
psycopg[binary]>=3.2.0            # Async Postgres driver (LangGraph PostgresSaver)
langgraph>=0.2.0                  # Conversation graph (memory + state)
langgraph-checkpoint>=2.0.0       # Checkpoint primitives
langgraph-checkpoint-postgres>=2.0.0  # Postgres-backed conversation memory
alembic>=1.13.0                   # DB migrations

# ── Auth (JWT + password hashing) ─────────────────────────────────────────────
pyjwt>=2.8.0                      # JWT encode/decode
pwdlib[argon2]>=0.3.0             # Argon2 password hashing (recommended)
